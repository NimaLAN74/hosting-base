# Run E2E stock monitoring checks (routes + API). No deploy; tests live site.
name: E2E Stock Monitoring

on:
  workflow_dispatch:
  push:
    branches: [master, main]
    paths:
      - 'lianel/dc/scripts/monitoring/e2e-stock-monitoring-routes.sh'
      - '.github/workflows/e2e-stock-monitoring.yml'

jobs:
  e2e:
    name: Stock routes + API
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: E2E Stock Monitoring (routes + API)
        run: bash lianel/dc/scripts/monitoring/e2e-stock-monitoring-routes.sh
        env:
          BASE_URL: ${{ vars.FRONTEND_URL || 'https://www.lianel.se' }}
